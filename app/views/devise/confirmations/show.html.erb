<%= content_for :body_id, 'confirmation' %>

<div id="signup">
  <section class="hero">
    <div class="signup-wrapper">
      <header>
        <h1>계정 활성화</h1>
        <h4>지금 나에게 필요없는 물건들을 나눠보세요</h4>
      </header>
      <section class="sign-in">
        <%= form_for resource, as: resource_name, url: update_user_confirmation_path, html: {method: 'patch', multipart: true}, id: 'activation-form', class: 'new_user' do |f| %>
          <%= hidden_field_tag :confirmation_token, @confirmation_token %>
          <div class="form-group">
            <%= f.label :username, class: 'control-label' %>
            <%= f.text_field :username, class: 'form-control' %>
          </div>
          <div class="form-group">
            <%= f.label :email, class: 'control-label' %>
            <%= f.email_field :email, class: 'form-control', disabled: true %>
          </div>
          <% if @requires_password %>
            <div class="form-group">
              <%= f.label :password, class: 'control-label' %>
              <%= f.password_field :password, class: 'form-control' %>
            </div>
            <div class="form-group">
              <%= f.label :password_confirmation, class: 'control-label' %>
              <%= f.password_field :password_confirmation, class: 'form-control' %>
            </div>
          <% end %>
          <br>
          <div class="form-group">
            <%= f.submit "가입하기", class: 'btn btn-primary col-xs-12' %>
          </div>
        <% end %>
      </section>
      <section class="text-center">
        <span>가입하기 버튼을 누르면 nanuGO의 <a href="/pages/terms">이용약관</a>에 동의하게 됩니다.</span>
      </section>
    </div>
  </section>
</div>
